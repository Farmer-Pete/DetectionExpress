/**
 * The fixed 960 x 600 design space the metro map is laid out in (view notes section
 * 1). The SVG uses it as a `viewBox`; the canvas fits and centers it uniformly. One
 * home so the two layers agree on the coordinate system.
 */
export const DESIGN_WIDTH = 960;
export const DESIGN_HEIGHT = 600;
