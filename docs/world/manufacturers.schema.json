{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://detectionexpress.game/schemas/manufacturers.schema.json",
  "title": "Sensor manufacturers",
  "description": "The vendors who build the station's sensors. Each one spells its data differently, which is what makes normalization a chore.",
  "type": "object",
  "additionalProperties": false,
  "required": ["manufacturers"],
  "properties": {
    "manufacturers": {
      "type": "array",
      "items": { "$ref": "#/$defs/manufacturer" }
    }
  },
  "$defs": {
    "manufacturer": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "name", "tagline", "description", "dataStyle", "makes"],
      "properties": {
        "id": { "type": "string", "pattern": "^[a-z-]+$" },
        "name": { "type": "string" },
        "tagline": {
          "type": "string",
          "description": "The line they would put on the side of the van."
        },
        "description": { "type": "string" },
        "dataStyle": {
          "type": "string",
          "description": "How their payloads read: key casing, timestamp format, how they encode a result. This is the normalization headache in one sentence."
        },
        "makes": {
          "type": "array",
          "description": "Sensor ids this vendor builds. Cross-reference sensors.json.",
          "items": { "type": "string" },
          "minItems": 1
        }
      }
    }
  }
}
